package Arrays.Wed.meetup.may10;

import java.util.*;

public class SplitString {

	public static void main(String str[]) {

		Set<String> set = new HashSet<String>();
		set.add("I");
		set.add("Like");
		set.add("Meet");
		set.add("Up");

		//List<String>  result = isStringExist("ILIKEMEETUP", set);

		/*if(result.size() == 0){

		}
*/
	}
//ISMEET
	/*public static boolean isStringExist(String str, Set<String> dic) {

		StringBuilder sb     = new StringBuilder();
		List<String>  result = new ArrayList<String>();
		for (int i = 0; i < str.length() - 1; i++) {
			sb.append(str.charAt(i));
			//Found the word
			if (dic.contains(sb.toString())) {
				//result.add(sb.toString());
				sb.setLength(0);
			}*//* else if (dic.isPrefix(sb.toString())) { //continue looking for word
				continue;
			}*//* else { // word doesnt exist return false;
				return false;
			}
		}

	}
*/

}

